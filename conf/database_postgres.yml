---
default: &default
  adapter: postgresql
  schema_search_path: public
  min_messages: warning
  username: delphix_u
  password: delphix
  host: <%= ENV["DB_HOST"] %>
  port: <%= ENV["PGS_DB_PORT"] %>
  
development:
  <<: *default
  database: ffcrm

production:
  <<: *default
  database: ffcrm
